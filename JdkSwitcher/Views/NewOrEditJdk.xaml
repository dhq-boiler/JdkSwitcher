<UserControl
    x:Class="JdkSwitcher.Views.NewOrEditJdk"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:JdkSwitcher.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:JdkSwitcher.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:utils="clr-namespace:JdkSwitcher.Utils"
    xmlns:viewModels="clr-namespace:JdkSwitcher.ViewModels"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ObjectDataProvider
            x:Key="EnvironmentVariableTargetComboBox"
            MethodName="GetValues"
            ObjectType="{x:Type utils:EnumerationManager}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="sys:EnvironmentVariableTarget" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <DockPanel LastChildFill="True">
        <StackPanel
            HorizontalAlignment="Right"
            DockPanel.Dock="Bottom"
            Orientation="Horizontal">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Width" Value="75" />
                    <Setter Property="Margin" Value="5" />
                </Style>
            </StackPanel.Resources>
            <Button Command="{Binding OkCommand}" Content="OK" />
            <Button Command="{Binding CancelCommand}" Content="キャンセル" />
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Label
                Grid.Row="0"
                Grid.Column="0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center">
                名前
            </Label>
            <TextBox
                Grid.Row="0"
                Grid.Column="1"
                Height="25"
                Text="{Binding Name.Value}" />
            <Label
                Grid.Row="1"
                Grid.Column="0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center">
                JAVA__HOME
            </Label>
            <TextBox
                Grid.Row="1"
                Grid.Column="1"
                Height="25"
                Text="{Binding JavaHome.Value}" />
            <Label
                Grid.Row="2"
                Grid.Column="0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center">
                EnvironmentVariableTarget
            </Label>
            <ComboBox
                Grid.Row="2"
                Grid.Column="1"
                Height="25"
                ItemsSource="{Binding Source={StaticResource EnvironmentVariableTargetComboBox}}"
                SelectedItem="{Binding EnvironmentVariableTarget.Value}" />
        </Grid>
    </DockPanel>
</UserControl>
